node_modules

images/*
!images/README.md
!images/test

# Ad-hoc input folders (e.g. a --inputDirectory pointed outside ./images) -
# real personal library content, never meant to be tracked.
input/

output/*
!output/README.md
# Featured in the README - kept as a tracked exception to the folder's
# otherwise-generated, gitignored contents.
!output/Morph 2026.gif

# Credentials - must never be committed
.env
.env.*
!.env.example

# Runtime state and fetch artefacts
.colorgrid-cache.json
*.part-*
output/spotify-fetch-report.json
