# Spotify Web API credentials for `colorgrid fetch`.
# Create an app at https://developer.spotify.com/dashboard, then copy this file
# to `.env` and fill in the values. `.env` is gitignored - keep it that way.
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
